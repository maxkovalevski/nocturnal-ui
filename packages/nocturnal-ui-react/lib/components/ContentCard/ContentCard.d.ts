import React, { FC } from "react";
export interface ContentCardProps {
    topView?: React.ReactNode;
    bottomView?: React.ReactNode;
}
export declare const ContentCard: FC<ContentCardProps>;
export default ContentCard;
